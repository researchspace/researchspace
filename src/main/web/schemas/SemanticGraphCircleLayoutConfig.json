{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "The circle layout puts nodes in a circle",
    "properties": {
        "animate": {
            "default": false,
            "description": "Whether to transition the node positions",
            "type": "boolean"
        },
        "animationDuration": {
            "default": 500,
            "description": "Duration of animation in ms if enabled",
            "type": "boolean"
        },
        "animationEasing": {
            "description": "Easing of animation if enabled. For possible values see `transition-timing-function` at [easing](http://js.cytoscape.org/#style/transition-animation)",
            "type": "string"
        },
        "avoidOverlap": {
            "default": true,
            "description": "Prevents node overlap, may overflow bounding-box and radius if not enough space",
            "type": "boolean"
        },
        "clockwise": {
            "default": true,
            "description": "Whether the layout should go clockwise (true) or counterclockwise/anticlockwise (false)",
            "type": "boolean"
        },
        "fit": {
            "default": true,
            "description": "Whether to fit the viewport to the graph",
            "type": "boolean"
        },
        "padding": {
            "default": 30,
            "description": "The padding on fit in pixels",
            "type": "number"
        },
        "radius": {
            "description": "The radius of the circle in pixels",
            "type": "number"
        },
        "sortBy": {
            "description": "Full IRI of the property which value can be used to order nodes. Property value should be some kind of number, e.g `xsd:integer`, or at least valid number in `xsd:string` literal.",
            "type": "string"
        },
        "startAngle": {
            "default": "3/2*Math.Pi",
            "description": "Where nodes start in radians",
            "type": "number"
        },
        "sweep": {
            "description": "How many radians should be between the first and last node (defaults to full circle)",
            "type": "number"
        }
    },
    "propertyOrder": [
        "fit",
        "padding",
        "avoidOverlap",
        "radius",
        "startAngle",
        "sweep",
        "clockwise",
        "sortBy",
        "animate",
        "animationDuration",
        "animationEasing"
    ],
    "type": "object"
}

