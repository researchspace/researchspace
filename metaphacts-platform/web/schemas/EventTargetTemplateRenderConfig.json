{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "properties": {
        "id": {
            "description": "Identifier which will be used as event target id.",
            "type": "string"
        },
        "template": {
            "description": "<semantic-link uri='http://help.metaphacts.com/resource/FrontendTemplating'>Template</semantic-link> that will be rendered with data passed as context variables.\n**The template MUST have a single HTML root element.**",
            "type": "string"
        }
    },
    "propertyOrder": [
        "id",
        "template"
    ],
    "required": [
        "id",
        "template"
    ],
    "type": "object"
}

