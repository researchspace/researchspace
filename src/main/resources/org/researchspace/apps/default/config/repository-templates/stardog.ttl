#
# platform configuration template for a Stardog repository
# accesed as SPARQL repository with basic authentication
#
@prefix config: <tag:rdf4j.org,2023:config/> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix mph: <http://www.researchspace.org/resource/system/repository#> .
@prefix stardog: <http://www.researchspace.org/resource/system/repository/stardog#> .


[] a config:Repository ;
   config:rep.id "default" ; # Replace with your repository ID or leave default if this is the main repository
   rdfs:label "Stardog SPARQL repository with basic authentication" ; # Replace with a meaningful description for this repository
   config:rep.impl [
      config:rep.type "researchspace:StardogRepository" ;
      config:sparql.queryEndpoint <http://mystardog:5820/myDB/query> ; # Replace with the URL of the SPARQL endpoint to submit queries to
      config:sparql.updateEndpoint <http://mystardog:5820/myDB/update> ; # Replace with the update endpoint
      mph:username "admin" ; # Replace with username to connect to the repository
      mph:password "admin" # Replace with password to connect to the repository
   ].
