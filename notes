1. Bulk import images
2. Make a new folder in digilib
3. Create a "generate features" button
4. Fetch all images from digilib folder
5. Run python script to gen feat.
6. Upload feats on digilib.

7. Create suggest button.
8. Sends name to python.
9. Run scoring.
10. 

5. On click send request to python function with arg as label of obj. Store in a separate server.
6. Create a display panel with top n images.
7. Be able to click one image and select it as objs image.



DirectFileUploader.tsx

Which graph does researchpsace make new entities in via interface

(currently multifile uploader will replace files with same name)



































When user uploads data, show list of sibjects, user clicks on a term to see all props related to it.