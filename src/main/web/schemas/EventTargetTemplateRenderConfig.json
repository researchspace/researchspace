{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
        "id": {
            "description": "Identifier which will be used as event target id.",
            "type": "string"
        },
        "template": {
            "description": "<semantic-link uri='http://help.researchspace.org/resource/FrontendTemplating'>Template</semantic-link> that will be rendered with data passed as context variables.",
            "type": "string"
        }
    },
    "propertyOrder": [
        "id",
        "template"
    ],
    "required": [
        "id",
        "template"
    ],
    "type": "object"
}

