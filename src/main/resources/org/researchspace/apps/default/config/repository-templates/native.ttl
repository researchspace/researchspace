#
# Sesame configuration template for a native RDF repository
#
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#>.
@prefix config: <tag:rdf4j.org,2023:config/> .

[] a config:Repository ;
   config:rep.id "default" ; # Replace with your repository ID or leave default if this is the main repository
   rdfs:label "Native store" ; # Replace with a meaningful description for this repository
   config:rep.impl [
      config:rep.type "openrdf:SailRepository" ;
      config:sail.impl [
         config:sail.type "openrdf:NativeStore" ;
         config:native.iterationCacheSyncThreshold "0"; # Threshold for syncing query evaluation iteration caches to disk, change if needed
         config:native.tripleIndexes "spoc,posc" # Triple indices (subject, predicate, object, context), modify as needed (add or remove indices)
      ]
   ].
